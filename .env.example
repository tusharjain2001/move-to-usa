# Copy to .env and fill in. Vite only exposes vars prefixed with VITE_.
# Google Maps JavaScript API key with the Places API enabled — powers the
# "Moving from" / "Moving to" city autocomplete in the quote band.
VITE_GOOGLE_PLACES_API_KEY=
